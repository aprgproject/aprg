name: Debug Workflow

on:
  workflow_dispatch:
    inputs:
      cppProjects:
        description: 'C/C++ Project ("all" for all projects)'
        type: string
        required: true
        default: 'AprgAlgorithm'

jobs:

  debugJob:
    name: Perform debug
    runs-on: ubuntu-latest
    steps:
        
      - name: Debug Steps
        shell: bash
        run: |
          gcc --version
          g++ --version
          cmake --version
          command -v gcc
          command -v g++
          command -v cmake
          #choco install cmake ninja
          #choco info cmake
          #echo "cmake version: [$(cmake --version)]"
          #choco info ninja
          #echo "ninja version: [$(ninja --version)]"
          #ninja --version
          #command -v cmake
          #command -v ninja
          #set +x
          #valgrind
          #valgrind -v
          #which gcc | xargs -t gcc --version
          #dpkg --list | grep compiler
          #set -x
          set +x
          find / -name "scan-build"
          set -x
          