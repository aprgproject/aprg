set(BENCHMARK_SOURCE_DIRECTORY
    ${BENCHMARK_DIR}/src
)

set(BENCHMARK_FROM_GIT_HUB_SOURCE_DIRECTORY
    ${BENCHMARK_FROM_GIT_HUB_DIR}/src
)

set(BENCHMARK_TEST_DIRECTORY
    ${BENCHMARK_DIR}/tst
)

set(BENCHMARK_FROM_GIT_HUB_TEST_DIRECTORY
    ${BENCHMARK_FROM_GIT_HUB_DIR}/tst
)

set(BENCHMARK_INCLUDE_DIRECTORIES
    ${BENCHMARK_SOURCE_DIRECTORY}
    ${BENCHMARK_FROM_GIT_HUB_SOURCE_DIRECTORY}
	${BENCHMARK_FROM_GIT_HUB_DIR}/include
)

set(BENCHMARK_TEST_INCLUDE_DIRECTORIES
    ${BENCHMARK_TEST_DIRECTORY}
    ${BENCHMARK_FROM_GIT_HUB_TEST_DIRECTORY}
)

PRINT_STATUS("Looking for benchmark sources C/C++ files in: [${BENCHMARK_SOURCE_DIRECTORY}]")
GET_SOURCE_FILES_FROM_DIRECTORY(BENCHMARK_SOURCES ${BENCHMARK_SOURCE_DIRECTORY})

PRINT_STATUS("Looking for benchmark from git sources C/C++ files in: [${BENCHMARK_FROM_GIT_HUB_SOURCE_DIRECTORY}]")
GET_SOURCE_FILES_FROM_DIRECTORY(BENCHMARK_SOURCES ${BENCHMARK_FROM_GIT_HUB_SOURCE_DIRECTORY})

PRINT_STATUS("Looking for tests C/C++ files in: [${BENCHMARK_TEST_DIRECTORY}]")
GET_SOURCE_FILES_FROM_DIRECTORY(BENCHMARK_TESTS ${BENCHMARK_TEST_DIRECTORY})

PRINT_STATUS("Looking for benchmark from git tests C/C++ files in: [${BENCHMARK_FROM_GIT_HUB_TEST_DIRECTORY}]")
GET_SOURCE_FILES_FROM_DIRECTORY(BENCHMARK_TESTS ${BENCHMARK_FROM_GIT_HUB_TEST_DIRECTORY})

set(BENCHMARK_SOURCES_AND_TESTS
    ${BENCHMARK_SOURCES}
    ${BENCHMARK_TESTS}
)
